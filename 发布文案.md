# avif tool（AvifForge）— SVT-AV1 驱动的本地 AVIF 图片转换器

> 把成堆的 PNG / JPEG，批量压成新一代 AVIF。纯本地、纯 CPU、拖进来就能开始。

**版本**：1.3.0 ｜ **平台**：Windows 10 / 11（x64）｜ **安装包**：59,707,392 B（≈57 MB，MSI，per-user，无需管理员）

**下载**：[`avif.tool.1.3.0.msi`](https://github.com/SapphireRapids/avif-tool/releases/download/v1.3.0/avif.tool.1.3.0.msi)
（SHA-256 `CBDA85B351833C4AD00C1015F271D0C2C46305BE8882903D3C8AF89BF625C9DE`）

---

## 1.3.0 变更

**默认并发数从 1 改为按机器自适应。** 旧版不管机器几核都只跑一个编码进程——压测显示
「一核一进程」的吞吐随核数线性扩展（8 核 5.8×、16 核 7.5×），默认 1 在 4 核机上只用到
1/4 算力（4.2 vs 14.1 files/s）。现在默认值 = `Clamp(min(CPU 核数, 内存水位), 4, 16)`：
并发上限同时受可用内存约束（每个编码进程预留 220 MB，以可用内存的 60% 为预算），
小内存机器自动收敛，不会把机器压爆；「并发文件数」下方常驻一行说明当前上限的由来
（如"已按可用内存将上限调整为 11"）。想回到单文件串行的旧行为，把并发改成 1 即可。
旧配置一次性自动迁移（settings.json 结构版本 3 → 4），无需手动操作。

**修复一个会静默丢文件的竞态。** 同一批里同时转 `IMG.png` 和 `IMG.jpg` 且并发 > 1 时，
两个任务会选中同一个输出路径、两个编码进程并发写同一个 `.avif`，最终只剩一个写入者的
有效输出——双方都报告成功，用户侧就是少一个文件（24 个任务实测只出 12 个文件，
第二轮跑同批输入才补齐）。1.3.0 改为**候选输出名进程内原子预留**：挑一个磁盘上不存在
且未被本批预留的名字，任务结束（成功/失败/取消）即释放；跨批次不保留，rename 续号
行为不变。修复后同批 24 个任务稳定产出 24 个文件，840 任务重名风暴 0 碰撞、0 遗漏。

**单文件耗时列附带 CPU 时间。** 系统休眠/挂起恢复后，按墙钟计量的单文件耗时会显示虚高
值（实测出现过 3999s 的伪影，实际中位数仅 1 秒多）。现在耗时列悬停可见
`CPU 0.27s` 一列作对照——休眠灌水时墙钟与 CPU 差几个数量级，一眼可辨；取消/失败路径
同样记录 CPU 时间。

**升级**：已装任意旧版本的用户直接再跑一次即可，不必先卸载。安装包开了
`AllowSameVersionUpgrades`，同版本号若重新出包也能直接覆盖，不会并列装出两份。

工程侧：压力回归 harness（`test\StressHarness`，直接走应用真实编码管线）与
核心×线程×并发矩阵 harness（`test\MatrixHarness`，亲和性限核）随源码入库；
GitHub Actions 每次推送自动执行 构建全溶液 → 引擎端到端测试 → 快速压力回归 →
无头冒烟。压测方法与完整数据见 [docs\avif-tool-压测报告.md](docs/avif-tool-压测报告.md)。

---

## 它是什么

avif tool 是一款免费、离线、开箱即用的 Windows 图片转换工具，专注于把 **PNG 和 JPEG**
批量转换为同样级画质、却小得多的 **AVIF** 格式。

它不做云上传、不搞账号、不开会员——文件留在你的磁盘上，转换发生在你自己的 CPU 里。
编码内核采用 **SVT-AV1**（AOMedia 开源 AV1 编码器），通过工业标准的
libavif 参考编码器 `avifenc` 驱动，结果与网页端、解码器生态完全兼容。

## 为什么选它

| | avif tool | 在线转换站 | 通用看图软件附带导出 |
|---|---|---|---|
| 批量处理 | ✅ 数千张，并发按核数/内存自适应 | ⚠️ 排队/限体积 | ⚠️ 逐张 |
| 隐私 | ✅ 全程离线 | ❌ 上传服务器 | ✅ |
| 编码器 | ✅ 锁定 SVT-AV1 | 多为 aom | 多为旧库或 x265 |
| 画质可调 | ✅ 0–100 连续可调 + Alpha 独立质量 | ⚠️ 有限 | ⚠️ 有限 |
| 安装 | 双击 MSI，确认一次，30 秒 | 无需安装 | — |

## 核心特性

- **SVT-AV1 专注内核**：软件内编码器锁定 SVT-AV1 v4.1.0。SIMD（AVX2 / AVX-512）
  与多核并行可由内核自动派发，「单图工作线程」填 0 即用满 CPU；默认保守取值，
  不默认吃满你的核，也不出任何命令行操作。
- **并发按机器自适应**：默认并发 = `Clamp(min(CPU 核数, 内存水位), 4, 16)`
  （压测最优区间：并发≈核数、每进程 `-j 1`，吞吐线性且延迟稳定）；内存水位按
  每进程预留 220 MB、可用内存 60% 预算计算，小机器自动收敛；并发与单图线程
  仍可手动任意设定，UI 常驻上限说明。
- **拖放式批量队列**：拖入文件或整个文件夹（可选递归子目录，默认开），队列实时显示
  每张图的输入输出体积、**节省百分比**与耗时（悬停附 CPU 时间），支持中途停止、
  失败重试、一键清除已完成。
- **看得懂的画质控制**：图像质量 0–100 连续滑杆（默认 **75**）、「最高质量档」
  （q=100，近无损）、Alpha 通道独立质量、编码速度 0–10 档（最省体积 ⇄ 最快完成）。
- **位深 8 / 10 bit，默认 10-bit**：天空等平滑渐变几乎不出现断层带，体积反而常更小。
- **输出自己定**：默认集中输出到「图片」文件夹，也可以填相对名作为原图下的子文件夹、
  或者留空与原图同目录；同名输出默认**自动改名** ` (2)(3)…`，不覆盖也不跳过；
  可选保留 EXIF/XMP/ICC 元数据，或转换成功后把原图移入**回收站**（走系统 Shell 接口，可还原）。
- **像素与并发全部可调**：色度采样固定 4:2:0（SVT-AV1 当前唯一支持路径），
  YUV 范围可选默认 / Limited / Full，并发文件数与单图工作线程均可按机器性子设定。
- **克制到底的生命周期**：关窗即释放——所有编码子进程随窗口关闭全部终止，
  无系统托盘、无服务、无自启动、无残留内存占用。
- **Fluent 原生界面**：WPF + Windows 11 视觉风格（Mica 云母材质），
  明暗主题可跟随系统，全窗口 Per-Monitor DPI 适配，高分屏清晰不发虚。
- **中文化细节**：队列状态、体积、节省率、错误信息均为中文，引擎报错会被
  精简成人话显示在对应文件行内。

## 使用方式（30 秒上手）

1. 双击 MSI 安装 → 桌面出现 avif tool 图标；
2. 把一堆 PNG/JPG 拖进窗口（或点「添加文件 / 添加文件夹」）；
3. 右侧滑一下质量（默认 **75**，摄影与截图都够用；想再压小，摄影降到 50–65、
   截图/线稿 60–75）；
4. 点「开始转换」，在原地看进度条跑完，收工。

## 质量参考

| 场景 | 建议 |
|---|---|
| 照片、壁纸 | 默认 75 即可；想再压小降到 50–65，速度 5–7 |
| 截图 / UI / 线稿 | 质量 60–75，速度 3–6 |
| 尽可能小、接受细节损失 | 质量 40–55 |
| 存档 / 二次编辑底稿 | 开「最高质量档」（q=100，近无损） |

> ℹ️ 技术说明：SVT-AV1 当前仅支持 4:2:0 色度采样（无 4:4:4 真无损，需换用其它编码器，
> 而本软件按设计只使用 SVT-AV1）。q=100 是该内核可达的最高画质档，对肉眼与
> 常规工作流而言已接近无损。

## 系统要求

- Windows 10（1809+）/ Windows 11，64 位
- 任意支持 x86-64 的 CPU（有 AVX2 更快，无则自动回退）
- 磁盘空间：安装约 70 MB
- **无需** .NET 运行库 / VC++ 运行库（运行时已内嵌，单文件发布）

## 技术信息（给硬核用户）

- 应用层：.NET 10 WPF + WPF-UI（Fluent）+ CommunityToolkit.Mvvm，自包含单文件；
- 编码层：libavif 1.4.2 官方源码静态构建（MSVC / Release / x64），
  唯一启用 SVT-AV1 v4.1.0 编码插件；构建配方与产物哈希公开可复现；
- 参数以独立 argv 数组传入子进程，天然命令行注入安全；
- 回收站删除使用 `shell32!SHFileOperationW`（FOF_ALLOWUNDO）；
- 崩溃日志写入 `%LOCALAPPDATA%\AvifForge\logs`，方便反馈问题时附带。

## 许可与开源致谢

avif tool 免费使用。它站在这些开源项目的肩膀上：
**SVT-AV1**（AOMedia, BSD-2+Patent）、**libavif**（BSD-2）、**libyuv / zlib / libpng /
libjpeg-turbo**、**WPF-UI**（MIT）、**CommunityToolkit.Mvvm**（MIT）、
**.NET**（MIT）。完整声明见应用安装目录内的
`THIRD-PARTY-NOTICES.txt`。

---

*AVIF · AV1 是 AOMedia 的格式与商标；本项目与 AOMedia 无隶属关系。*

---

## 版本历史

- **1.3.0** — 默认并发从 1 改为按机器自适应（`Clamp(min(核数, 内存水位), 4, 16)`；
  压测：一核一进程吞吐随核数线性，旧默认在 4 核机只用到 1/4 算力），上限同步受内存
  约束（每进程预留 220 MB，UI 有说明）；**修复同名基件并发竞态**（同批 `IMG.png` +
  `IMG.jpg` 并发时两个任务写同一输出，静默丢一份文件——候选输出名改原子预留，
  24 任务 12 文件 → 24 文件）；耗时列附 CPU 时间（免疫休眠墙钟灌水）；旧配置一次性
  迁移（settings.json schema 3 → 4）。工程侧：StressHarness / MatrixHarness / CI 入库。
- **1.2.1** — 重做安装确认框的字形与排版。中文界面改用显式的 **Microsoft YaHei UI**：
  此前不指定字体时，中文字符走字体链接回退，被渲染成宋体一类字形，和拉丁字形的粗细
  明显不齐——这就是「确认框字体丑」的来源。同时按现代网格重排版式（标题 + 分隔线 +
  正文 + 是/否），去掉画不正确的图标控件。程序功能与 1.2.0 完全一致，
  已装 1.2.0 的用户直接再跑一次即可升级，不必先卸载。
- **1.2.0** — 默认质量 60 → 75，摄影与截图都更稳的开箱效果；任务完成后「输出体积」列
  即时刷新；MSI 增加安装确认对话框。缺陷修复：移除 UI 里必然失败的「12 bit」档位
  （旧配置自动迁移为 10 bit）、编码中途失败不再残留半截 `.avif`、修复 `nuget.config`
  的无效 XML（此前新克隆的仓库根本无法还原包）。队列统计改为增量维护，
  拖入上千张图界面不再抖。
- **1.1.0** — 产品改名 avif tool：新的安装目录、开始菜单/桌面快捷方式名与
  UpgradeCode，与旧版 AvifForge **完全独立**，不会互相卸载或串台。
- **1.0.2** — 默认输出目录改为「图片」文件夹；已存在时默认自动改名；默认保留
  EXIF/XMP/ICC 元数据；默认并发 1。
- **1.0.1** — 默认位深改为 10-bit（修复平滑渐变/天空场景可见断层带；实测同画质最大
  亮度台阶 29 级 → 21 级，配合 q80 可降至 6 级且文件常更小）；旧配置自动迁移，
  用户显式选择保持不变。
- **1.0.0** — 首个正式版：SVT-AV1 批量转换、拖放队列、质量/速度全参数 UI、
  回收站删除、Fluent 界面、per-user MSI。
